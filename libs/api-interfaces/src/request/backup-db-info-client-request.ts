/**
 * Client request type for backupdbinfo (DB backup physical info).
 *
 * @category Client Requests
 * @since 1.0.0
 */
export type BackupDbInfoClientRequest = {
  /**
   * Database name
   */
  dbname: string;
};
