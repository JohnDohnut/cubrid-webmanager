/**
 * Client response type for restoredb task.
 *
 * @category Client Responses
 * @since 1.0.0
 */
export type RestoreDbClientResponse = {
  __EXEC_TIME?: string;
  note: string;
  status: string;
  task: string;
};

