/**
 * Client response type for addbackupinfo request.
 * Empty object on success (CMS envelope fields removed).
 * 
 * addbackupinfo 요청에 대한 클라이언트 응답 타입입니다.
 * 성공 시 빈 객체를 반환합니다 (CMS 메타 필드 제거).
 * 
 * @category Client Responses
 * @since 1.0.0
 */
export type AddBackupInfoClientResponse = {};

