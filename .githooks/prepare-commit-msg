#!/bin/sh

commit_msg_file=$1

if [ -z "$commit_msg_file" ] || [ ! -f "$commit_msg_file" ]; then
  exit 0
fi

tmp_file=$(mktemp)
sed '/^[Cc]o-authored-by:.*[Cc]ursor.*/Id' "$commit_msg_file" > "$tmp_file"
mv "$tmp_file" "$commit_msg_file"

exit 0
